export const metadata = {
  title: 'Submit Video',
  description: 'Add video to the home page carousel',
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
